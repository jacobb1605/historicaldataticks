- Create SQLITE database
- automate tick data pulls via python. You want your command to be:
    Python main_bacltest.py --ticker=abc --start_date end date....
    where the program will pull the data and run the tests
All data pulled needs to be saved in a sqlite database. If data already exists, do not pull
Handle duplicates
